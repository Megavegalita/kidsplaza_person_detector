[pytest]
# Discover tests under repository tests/
testpaths = ../tests
# Ensure src/ is on sys.path
pythonpath = ../src
# Exclude template scaffolding from discovery
norecursedirs = templates
# Quiet mode by default and ignore templates
addopts = -q --ignore=templates
